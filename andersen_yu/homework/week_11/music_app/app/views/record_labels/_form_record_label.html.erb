<div class="col-md-6 col-md-offset-3">

 	<%= simple_form_for(@record_label, html: {:class => 'form-horizontal'}) do |f| %>
		 <div class="well">
		 	<%= f.input :name, input_html: {:class => 'form-control', placeholder: 'Record Label Name'} %>
		</div>
		<br>
		<div style="text-align:center;">
		 	<%= f.submit "Submit".html_safe, :class => 'btn btn-lrg btn-danger' %>
		 </div>
 	<% end %>
 </div>
 </div>
